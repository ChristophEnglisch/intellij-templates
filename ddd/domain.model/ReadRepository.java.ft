package ${PACKAGE_NAME};

import java.util.Collection;

@org.jmolecules.ddd.annotation.Repository
public interface ${NAME}ReadRepository {
    Collection<${NAME}> findAll();
    ${NAME} findById(${NAME}Id ${NAME.toLowerCase()}Id);
}